# Implementation Plans

Index of every advisor plan in this repo, newest suite last. Jump to the
suite you need:

- **000–013 — Character Studio** (2026-07-02, complete; the studio itself was
  archived 2026-07-23 via plan 042 — tag `archive/character-studio-2026-07-23`)
- **014–032 — Island editor + engine island quality** (2026-07-12 → 07-23, complete)
- **033–042 — Demo readiness** (2026-07-23, complete)
- **043–068 — Product hardening audit** (2026-07-25, TODO) — see
  [2026-07-25 audit suite](#2026-07-25-audit-suite-043068)

Each executor: read your plan fully before starting, run its drift check,
honor its STOP conditions, and update your row in the relevant table.

---

## Character Studio suite (000–013)

Generated by the improve skill (plan variant) on 2026-07-02, against commit
`69df998`. This suite designs and builds the **Character Studio**: an internal
tool for authoring Animal Crossing / Pokémon Pokopia-grade animated 3D animal
companions. **Read `000-architecture-and-strategy.md` before executing any
plan** — it holds the research-grounded technical strategy, the rejected
alternatives, the canonical skeleton and CharacterSpec vocabularies, and the
working agreements every executor must follow.

Each executor: read your plan fully before starting, run its drift check,
honor its STOP conditions, and update your row below when done.

## Execution order & status

| Plan | Title | Phase | Priority | Effort | Depends on | Executor model | Status |
|------|-------|-------|----------|--------|------------|----------------|--------|
| 000 | Architecture & technical strategy | — | — | — | — | (reference doc) | N/A |
| 001 | Workspace scaffold + render baseline | 1 | P1 | M | — | Sonnet 5 | DONE (merged to `feat/character-studio` as `b7b742f`) |
| 002 | Drawn-face system | 1 | P1 | L | 001 | **Fable 5** | DONE (merged to `feat/character-studio` in `aa83f08`) |
| 003 | Spring-bone secondary motion | 1 | P1 | L | 001 | **Fable 5** | DONE (merged to `feat/character-studio` in `aa83f08`) |
| 004 | CharacterSpec data model | 2 | P1 | M | 002, 003 | Sonnet 5 | DONE (merged to `feat/character-studio` at `f1631cf`, incl. 관상 personality) |
| 005 | Toon rendering & materials | 2 | P1 | L | 001, 002, 004 | **Fable 5** | DONE (merged to `feat/character-studio` at `ed2f6df`) |
| 006 | Skeleton, archetypes & anatomy parts | 2 | P1 | L | 002–005 | **Fable 5** + Blender MCP | DONE (merged to `feat/character-studio` at `5d9d792`; operator approved visuals + plan-000 §5 shoulder amendment) |
| 007 | Animation clips & Play Mode | 3 | P1 | L | 003, 006 | **Fable 5** + Blender MCP | DONE (merged to `feat/character-studio` at `0498c14`; operator approved soak/settle/talk videos; 60 fps feel check remains a standing note) |
| 008 | Wardrobe & accessory system | 3 | P2 | L | 006, 007 | **Fable 5** + headless Blender | DONE (merged to `feat/character-studio` at `fd93885`; operator approved dressed-motion videos + the slimmer post-morph-fix silhouette) |
| 009 | Freeform sculpt, lattice & undo | 4 | P2 | L | 004, 006 | **Fable 5** | DONE (merged to `feat/character-studio`; lead ran step-6 gate — sculpt survives morphs/archetype-round-trip/Play-Mode, undo lossless, geodesic pick 0.08–1.30 ms; operator approved) |
| 010 | Lighting studio | 4 | P2 | M | 004, 005 | Sonnet 5 | DONE (merged to `feat/character-studio` at `384f161`; operator approved presets + terminator sweep; gizmo mouse-drag needs a one-time human check) |
| 011 | Export & companion-runtime | 5 | P1 | L | 004–009 | Opus 4.8 | DONE (merged to `feat/character-studio`; operator approved — CLI+panel export → 0.51 MB conformant GLB, generic GLTFLoader self-sufficient, companion-runtime version-agnostic on three 0.149∧0.185; studio 420 tests, runtime 25) |
| 012 | Studio shell & roster | 4 | P2 | M | 004 (+landed panels) | Sonnet 5 | DONE (merged to `feat/character-studio` at `6f01a44`; operator approved shell + roster reload-survival; gates green, 420 tests) |

Status values: TODO | IN PROGRESS | DONE | DONE-pending-visual (code gates
green, aesthetic/motion gate awaiting human view) | BLOCKED (one-line reason)
| REJECTED (one-line rationale).

**Session handoff (updated 2026-07-04): ALL 12 PLANS COMPLETE.** Plans 001–012
executed, reviewed, and merged to `feat/character-studio`. The suite is done:
authored archetype bodies + anatomy parts on one canonical skeleton, 관상 face
atlases, toon rendering, spring secondary motion, a full animation clip set +
Play Mode, wardrobe, freeform sculpt + lattice, a lighting studio, the studio
shell + IndexedDB roster, and the export pipeline + version-agnostic
`companion-runtime` (validated on three 0.149∧0.185). Post-merge stitch DONE:
each RosterView card now has an **"Export .glb"** action that parses the saved
spec and compiles a runtime `.companion.glb` in-browser via a shared
`companionExport.ts` helper (the same path ExportPanel uses for the live
character) — verified end-to-end (download fires, 434 studio tests still
green). **Next work is the queued Fable-led
integrated aesthetic polish pass** (a future session — Fable was rate-limited
here): hero sculpt for demos, tighten the AC/Pokopia silhouette bar on authored
assets, motion feel, lighting presets. See
[[project-character-studio-execution-status]] in memory. Model note: **Fable 5 is rate-limited this
session** — if unavailable, aesthetic-gated continuations can run on Opus 4.8
(the lead ran 009's step-6 gate directly on Opus). New human-check debt:
light-gizmo mouse-drag (store path verified; drag not simulatable in
automation). Sculpt facts for 011/012: sculpt deltas persist in
`spec.anatomy.sculptDelta` (sparse, quantized 1e-5 m, version-guarded);
`applyDelta` writes base+delta into `position` so morphs/skinning compose in
shader; whole-character lattice bakes into the same delta layer; switching
archetype re-applies deltas from spec via `syncTargetsToPayload` (dormant
layers for unequipped parts are preserved). **Plan 011 export must bake
`basePositions+delta` into exported geometry** (GLB self-sufficient; spec
keeps the editable delta) AND still zero the plan-006 part morph defaults.
Blender note: MCP live addon never connected this session; headless Blender
5.1.2 works; all assets regenerate (`pnpm gen:assets` — bodies/parts/wardrobe
— and `scripts/blender/clips.py`). Facts new executors need: bodies now have
hide-region submeshes (torso/hips/upperLegs); wardrobe dressing lives in
`src/core/wardrobe/` and CharacterRoot reassembles on wardrobe changes;
**plan-006 anatomy part GLBs still ship shape keys defaulted to weight 1**
(bodies regenerated clean in 008; parts are runtime-neutralized in
assemble/dress/CharacterRoot — plan 011's export MUST zero morph defaults or
regenerate parts); spring chains authored along a surface need plumb bones +
colliders (hanging chains equilibrate vertically); triage SkinnedMesh
occlusion bugs as morph-influence data bugs first. Execution pattern:
dispatch an executor subagent per plan (isolated worktree branching from
current `feat/character-studio` HEAD, model per this table, inline the full
plan text in the prompt), review like a tech lead (re-run gates in the
worktree, scope-check the diff, audit tests, judge look/motion screenshots
saved to the session scratchpad), merge only with operator approval, keep
this table current. Executors die at session-limit walls: require per-step
commits; continuation executors inherit the worktree (SendMessage resume has
never worked — transcripts expire). Operator directions in force: quality
over cost (premium executors for aesthetic-gated plans), 관상 faces,
screenshots/videos to the operator (often on mobile — webm only, no system
ffmpeg) with every visual verdict. Known debts: ~~panel overlap + FacePanel
style warning~~ (resolved by 012), `Math.random` core guard test missing
(rule honored manually), absolute-60fps check pending operator's laptop,
body-level boneScales piggyback on part entries (plan-004 schema gap;
revisit 009/012), shell-union bodies want a human sculpt pass at
shoulders/hips (ASSET-CONTRACT.md), hood↔backpack overlap when both worn,
drawstrings bury at extreme bellyRound, mug grazes thigh in profile, scarf
tips read stiff at rest. **New from 012**: MotionDebugPanel (plan 003, dev
spring-tuning) stays a fixed-position floating overlay outside the ModeTabs
column by design — it's not one of the 7 builder-flow modes and its own
doc comment already marks it provisional; RosterView's rename/delete use
native `window.prompt()`/`confirm()` rather than a bespoke modal (matches
the "no UI framework" constraint, but is the crudest possible affordance —
worth a real modal if the roster grows past a quick internal tool); the
gizmo-hide-during-thumbnail-capture trick (thumbnails.ts) depends on React
committing a re-render before the next r3f `useFrame` tick, which held up
in testing but is timing-shaped, not structurally guaranteed; Toasts'
z-index (300) is hand-kept above the roster overlay's (200) — bump both
together if either changes; no in-app UI surfaces the autosave-slot count
or lets a designer browse older slots (only the crash boundary's "revert to
last" is wired) — `rosterStore.countAutosaveSlots`/the `autosaveSlots`
store exist for a future "restore points" affordance. Plan 011 note: 012
did NOT add a ".companion.glb export" action to RosterView (011 not landed
at time of writing) — 011 should add it there once it exists.

**Executor model rationale**: Fable 5 wherever the plan's success gate is
aesthetic or judgment-based (face, springs, sculpt, look, authored assets,
animation feel — per operator direction 2026-07-02: quality over cost, never
silently downgrade); Opus 4.8 for heavy precedented engineering with
machine-checkable gates (export/runtime); Sonnet 5 for well-specified
scaffold/schema/UI work. Haiku is not recommended anywhere in this suite.
Plans 006/007 want Blender MCP access for asset authoring (each has a
BLOCKED fallback without it).

## Phases & milestones (gate each phase before the next)

1. **Prove it lives** (001–003): placeholder character with a drawn face that
   blinks and watches the cursor, ears/tail that lag/overshoot/settle, visible
   breathing. *Go/no-go gate for the whole project.*
2. **A real character** (004–006): dog and bird assembled from authored parts,
   palette-recolored, Pokopia-grade shading, still alive.
3. **Motion & identity** (007–008): full Play Mode (idle/walk/run/sit/talk/
   gestures + soak test); dressed characters whose clothing moves.
4. **Authoring power** (009, 010, 012): unique silhouettes beyond sliders,
   relightable stage, roster persistence. (009 ∥ 010 ∥ 012 are parallelizable.)
5. **Ship the roster** (011): exported `.companion.glb` plays with full
   secondary motion in a three@0.149-compatible runtime package.

## Dependency notes

- 002/003 are independent of each other; run in parallel after 001.
- 004 imports vocabularies created by 002 (`atlas.ts`) and 003
  (`springTypes.ts`) — it must not fork them.
- 006 is the suite's keystone: bodies/skeleton feed 007/008/009; its rest
  pose is contractual for 007's clips (changing it later re-exports all clips).
- 011 executes last but is P1 — it is the product payoff; its conformance
  suite is the fidelity gate for everything upstream.
- 012 composes whatever panels have landed; only 004 is a hard dependency.

## Standing constraints (from 000 — repeated because they get violated)

- `character-studio/` is an **isolated pnpm root** (bird-builder pattern).
  Never add it to root `pnpm-workspace.yaml`; never touch root `overrides`.
- `src/core/**` never imports React (mechanically enforced by a plan-001 test).
- Frame order: animation → physics(springs) → procedural → render. Never reorder.
- Face planes are always unlit. Spring-bone chains are never keyframed.
- No `Math.random` in core (inject seeded RNG). No retargeting anywhere.
- Fixed mesh topology (no dyntopo). meshopt+KTX2, not Draco. GLB self-sufficient
  for naive viewers; `SEN_companion` extension carries the "alive" data.

## Standalone advisor plans (outside the Character Studio suite)

Later improve-skill runs add plans here with monotonic numbering. These do NOT
belong to the Character Studio suite above — each is independent unless its own
"Depends on" says otherwise.

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 014 | Island editor: lower beach tier flush with the sea | P2 | S | — | DONE (merged to `feat/island-editor-v2` @ 696a321 ff, 2026-07-12) |
| 015 | Island editor: build new Meshy assets (tree v2, grass, character) | P1 | M | — | DONE (merged to `feat/island-editor-v2` @ 696a321 ff; tree source became tree-2.glb mid-run, budget re-baselined 850 KB) |
| 016 | Island editor: drag-painted grass replaces the dirt path (spec v5) | P1 | L | 015 | DONE (merged to `feat/island-editor-v2` @ 696a321 ff) |
| 017 | Island editor: one animated character + collision + clip cycler | P1 | L | 015 (run after 016) | DONE (merged to `feat/island-editor-v2` @ 696a321 ff) |
| 018 | Island editor: render placed objects unlit (KHR_materials_unlit) | P2 | S | 015–017 | DONE (merged @ 696a321; **superseded by 019** — maintainer chose BOTW toon over unlit after seeing it) |
| 019 | Island editor: BOTW toon lighting on objects + tree-3 retexture | P1 | M | 014–018 merged | DONE (merged to `feat/island-editor-v2` @ 40db083 ff, 2026-07-12; amendment: tree budget → 1000 KB — tree-3's re-atlas splits 88,542 verts, geometry irreducible; visual orbit check pending human view) |
| 020 | Island editor: procedural BOTW grass blades + shader wind (drops grass.glb) | P1 | M-L | 019 (execute after — shared files; supersedes 019's grass bits) | DONE (merged to `feat/island-editor-v2` @ 1ae1ce7 ff, 2026-07-12; grass.glb retired; in-browser paint/wind/perf check pending human view) |
| 021 | Island editor: thinner, denser grass blades + cliff-face clip (maintainer feedback) | P1 | S | 020 merged | DONE (merged to `feat/island-editor-v2` @ 9faf64b ff, 2026-07-12; BLADE_W 0.018, 48/cell, CLIFF_DROP 0.05; verified via headless screenshot — cliff faces clean) |
| 022 | Island editor: gentle height-proportional grass wind + distance-stable blade width (maintainer feedback) | P1 | S | 021 merged | DONE (merged to `feat/island-editor-v2` @ bedb66b ff, 2026-07-12; lean ratio 0.12, widen 8→30 ×3.5; headless-verified — grass visible at max zoom-out, sway gentle) |
| 023 | Island editor: save/load island state to a repo-tracked file (maintainer feature request) | P1 | M | 021 merged (independent of 022 — no shared files) | DONE (merged to `feat/island-editor-v2` @ f7e346d ff, 2026-07-12; headless-verified end-to-end — Save writes saves/island.json, Reset → starter, Load restores) |

| 024 | Island editor: BOTW grass v2 — soft-edged sharp blades, per-blade wind, character bend + fade disc (ref: "All Zelda BOTW grass techniques revealed") | P1 | M | through `74e9392` | DONE (merged to `feat/island-editor-v2` @ 008b505 ff, 2026-07-12; 214 tests; browser pass by reviewer below) |
| 025 | Island editor: autonomous character — wander/hi/sleep/wake, swim + shore leash, click-to-talk, dock keeps manual override ('Auto' entry) | P1 | L | **024 merged** (GrassLayer uCharPos goes live-tracking; CharacterActor untouched by 024) | DONE (merged to `feat/island-editor-v2` @ 7d465d8 ff, 2026-07-12; 223 tests; wander + "Auto 1/11" dock verified headless; click-to-talk, swim leash, and sleep cycle are machine-tested — real-time human view still worthwhile) |

| 026 | Island editor: click-to-move command (camera-mode click) + stops sleep→wake instead of hi wave | P1 | M | 025 merged | DONE (merged to `feat/island-editor-v2` @ 2fcc200 ff, 2026-07-12; 228 tests; executor also fixed a latent gotoPending-across-talk bug) |
| 027 | Island editor: swim wake ripples + smooth swim transitions (hysteresis + vertical blend — fixes "patchy" swim restarts) | P1 | S | **026 merged** (shares CharacterActor; mirrors its draught condition into characterPose.swimming) | DONE (merged to `feat/island-editor-v2` @ 4c60fe8 ff, 2026-07-12; 232 tests) |
| 028 | Island editor: smooth terrain silhouette (C1 smooth-bilinear tier sampling) + sand-only shoreline (uBeachTop cliff gate) | P1 | M | **027 merged** (materials.test.ts overlap) | DONE (merged to `feat/island-editor-v2` @ 85eb7c4 ff, 2026-07-12; 233 tests, zero expectation changes — invariants held) |
| 029 | Island editor: FPS/resource HUD + edit-path perf (dedupe double shore-BFS, per-spec WeakMap caches, allocation-free grass scatter; zero visual change) | P1 | M | **027 + 028 merged** (CharacterActor/SeaSurface/terrain overlap) | DONE (merged to `feat/island-editor-v2` @ 5a6e5a9, 2026-07-12; 236 tests; grassField determinism tests passed UNMODIFIED — no-visual-change confirmed, and a pixel diff vs the pre-029 frame is empty outside the HUD, the wind-animated grass and the wave foam) |
| 030 | **Engine (product app)**: island close-up quality — retina DPR fix (strong dpr-2 machines boot tier `high`, not `medium`/1.5×) + terrain `SEGMENTS` 256→512 (maintainer "pixelated and blocky" screenshot; plan 028's sampler fix was already ported — these are the two remaining causes) | P1 | S | none (engine-side; editor untouched) | DONE (merged to main @ ae7cd35b, 2026-07-23; 1 revision round — plan rev 1 added a timeout-only amendment to islandGeometry.test.ts, whose per-vertex loops scale with SEGMENTS²; reviewer re-verified check + 72 targeted tests in the worktree; browser pass on a retina display still recommended) |
| 031 | **Engine + editor data**: curved coastline — one-time resample of the saved island 64×64→128×128 (threshold of a strongly-blurred field bakes curves into the data; runtime smoothing can't fix multi-cell staircase steps because BLUR_MIX keeps 60% raw by design) + BLADES_PER_CELL 64→16 density compensation + `pnpm sync:island` regen | P1 | M | **030** (stacks on `advisor/030-island-closeup-quality`) | DONE (merged to main @ 34c38fba + 4ce2e12e, 2026-07-23; rev 1 authorized 4 resolution-literal test updates, rev 2 de-flaked the ~1M-expect() per-vertex geometry tests to aggregated counters — 5-file combo run 810 ms) |
| 032 | **Engine + editor**: curved contours at runtime — retune tier-field sampler BLUR_PASSES 2→4, BLUR_MIX 0.4→0.85 (reviewer's offline contour study: steps stay visually hard until raw weight < ~0.2; invariant REDEFINED with verified numbers after executor STOP: single cell sinks below sea [intentional, matches "big smooth masses" art direction], 2×2 = old single cell stays visible beach-height land, raised bumps need ≥5×5 ≈ 0.94u; reviewer confirmed the visual win live before authorizing; rev 2 extended the floor SYMMETRICALLY to carved water — 7×7 pond pinned as the smallest surviving pocket — and a final adjudication nudged default-layout fruit-0 z 0.1→0.2 [both islandLayout.js AND the authoritative defaultIslandLayout.json]) | P1 | M | **031** | DONE (merged to main @ 4e01a6e1, 2026-07-23; reviewer re-verified gates + 85 targeted tests + live browser pass — coastline reads as smooth curves at all zooms) |

Post-plan direct work (operator-directed, executed by the advisor inline,
2026-07-12, commits `77ab254`/`2c4218c`/`74e9392`): gusty rotation-bend grass
wind (some blades whip near-flat; `uGustBend`), zoom-out grass HIDING
(supersedes 022's always-visible widen — widen now 8→20 ×2.5, hide 22→32;
the operator wanted declutter, not permanence), a hotbar Camera orbit tool
(sticky hold-Space, pan disabled), and the operator's saved island committed.

Post-029 direct work (operator-directed, commit `e80d6ed`): the bird kept the
swim clip and draught while walking on dry sand — plan 027's water hysteresis
put WATER_EXIT (seaLevel + 0.07) ABOVE the beach tier's top (0.05), so no sand
cell could ever clear the exit bar and a beached bird never came ashore. The
band now brackets the waterline (enter −0.02, exit +0.02), same 0.04 width, so
the anti-flip-flop guarantee survives; a regression test pins the beached case
at the exact tier-1 height. Same commit softened 027's swim wake (narrower ring
crest, falloff out by ~0.85 rather than 1.2 units, mix weight 0.70 → 0.30).

Further post-029 direct work (not previously logged here): commit `eea909ad`
raised the hotbar tooltips above the animation dock (z-order); commit `0e4122b6`
made stops an IDLE (stand + breathe) with only an occasional nap (`NAP_CHANCE`
0.25) instead of plan 026's every-stop sleep, and guaranteed a swim always ends
on land (no idle/nap at sea). Latest fix (this change): a SWIMMING bird sat at a
fixed draught `seaLevel − SWIM_SINK` (−0.12) that ignored the seabed, so on the
approach to shore its body was buried up to ~0.14 u (a quarter of the chick)
inside the rising beach until the swim→walk exit fired (ground > seaLevel +
0.02) — "swimming into the island." The draught is now clamped to the ground
beneath it (`bodyTargetY` in characterBehavior.ts), so the bird rides up the
sand instead of through it and the swim→walk hand-off is vertically continuous;
a unit test pins the never-below-ground invariant. Verified headlessly against
the saved island: every one of 12 shore approaches buried the body 0.11–0.14 u
before, 0.000 u after.

### 2026-07-23 demo-readiness suite (033–042)

Generated by the improve skill on 2026-07-23 against commit `a9e1364e`
(operator focus: "improve the app for demo purpose mainly"). All target the
**product app** (`src/`, `test/`), not the side studios. Recommended
execution order is the numeric order below, except: 034 requires 033; 037
and 038 both touch `SettingsSheet.tsx`, run sequentially (037 first); 042 is
independent and can run any time.

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 033 | Restore retry-sync UI for failed voice captures (regression; dropped in July history-sheet refactor) | P1 | M | — | DONE (2026-07-23, reviewer-approved, merged to main @ e88a94ee) |
| 034 | Fix the stale test gate — update 9 stale tests so `pnpm test` is green on main | P1 | M | 033 | DONE (2026-07-23, reviewer-approved, merged @ 48723a52; full suite green in worktree — 889 passed / 0 failed) |
| 035 | Relative demo seed dates — shift the corpus at seed-load so Alice's newest entry is always "yesterday" (fixture stays verbatim) | P1 | M | — | DONE (2026-07-23, reviewer-approved, merged @ d214f43c; run `pnpm demo:reset` once to refresh the local DB with anchored dates) |
| 036 | Singapore-timezone date bucketing — shared SGT day-key helper replaces UTC `toISOString().slice(0,10)` and local-time `ymd()` | P1 | S | — | DONE-pending-merge (2026-07-23, reviewer-approved @ worktree commit `965fe2dc`; merge BLOCKED by concurrent uncommitted CalendarPane/HistorySheet edits on main — merge branch `worktree-agent-a16ebcf4b0f622079` once that work is committed) |
| 037 | Demo operator kit — accurate README runbook, `.env.example`, `demo:reset`/`demo:bootstrap` scripts, PORT-overridable dev server | P1 | S–M | — | DONE (2026-07-23, reviewer-approved, merged @ 8d9caddc; note: `.env.example` is gitignored by repo convention — template copied to main tree untracked) |
| 038 | Demo persona switcher — settings-sheet control that re-mints the demo cookie for demo-a…d | P2 | M | 037 (shared `SettingsSheet.tsx`) | DONE (2026-07-23, reviewer-approved, merged @ d6537519; full suite on main 905 passed / 0 failed) |
| 039 | Hover CTA anchor without React re-renders — direct-DOM `left`/`top` per the `use-world-position` pattern | P2 | S | — | DONE (2026-07-23, reviewer-approved, merged @ `dcbbaaf8`) |
| 040 | Lazy-load debug deps — `lil-gui`/`stats.js` dropped from the prod engine bundle via conditional top-level-await import | P3 | S | — | DONE (2026-07-23, reviewer-approved, merged @ `0bb7d1b8`; prod bundle verified free of lil-gui/stats.js) |
| 041 | SPIKE: Connector at capture time behind `VITE_DEMO_CONNECTOR_AT_CAPTURE` — live reflection visibly updates the Profile | P1 | M | best after 034 (green gate) | DONE (2026-07-23, reviewer-approved, merged @ 22c3b77e; live latency/cost measurement BLOCKED on env — run with VITE_DEMO_CONNECTOR_AT_CAPTURE=1 + DATABASE_URL to complete; write-up at docs/solutions/2026-07-23-connector-at-capture-spike.md) |
| 042 | Archive the Character Studio — recovery tag at main HEAD (mandatory: `feat/character-studio` does NOT contain the current copy), then `git rm -r` + script cleanup | P2 | S | — | DONE (2026-07-23, reviewer-approved, merged @ `1cf1e961`; tag `archive/character-studio-2026-07-23` created — PUSH IT on next push) |

Suite dependency/interaction notes:

- 033 before 034: the one failing test that documents real behavior
  ("can retry failed local reflection syncs") becomes green via 033; the
  other 9 failures are stale tests 034 rewrites against the shipped UI.
- 035 and 036 interact (fixture timestamps near midnight UTC change visible
  day under SGT bucketing) but have no hard dependency; each plan's
  maintenance notes cover the other.
- 041 is a spike: deliverable is a flag-gated prototype + a findings note in
  `docs/solutions/`, answering latency/cost/failure/idempotency questions
  before any production decision.

Findings considered and rejected / deferred (2026-07-23 audit — don't re-audit):

- Per-frame recursive raycast broad-phase (`WorldInteractions._pick`) and
  History Growth-tab fetch waterfall — real but lower leverage; deferred,
  not planned.
- GLB preload hints for `tree.glb`/`character.glb` — deferred; severity
  depends on venue network, measure first.
- "Restart onboarding" persistence race — downgraded: `Persistence.js`
  registers its own beforeunload flush, so the navigation drains pending
  writes; only the swallowed-error logging is worth fixing (folded into 037).
- Duplicate `reviewText` writers in `AskSheet.tsx` (:536 vs :927-929) —
  low-confidence smell, needs a live-transport repro before planning.
- Console-noise gating behind a debug flag — not worth doing; all sites are
  error-path-only and namespaced.
- Direction ideas D2 (sprout species by VIPS dimension), D3 (Character
  Studio companion on the island — explicitly rejected by operator, studio
  archived via 042), D4 (second full persona) — deferred by operator choice
  2026-07-23.

### 2026-07-25 audit suite (043–068)

Generated by the improve skill on 2026-07-25 against commit `031d1974`
(operator selection: all four finding bundles plus all three direction
findings, with two standing riders — **do not regress ease of use, speed, or
performance**, and **improve code quality / DRYness**). Every plan targets the
**product app** (`src/`, `test/`, config), not the side studios. Findings were
produced by four parallel category audits and then re-verified against the
code by the advisor before planning.

Baseline at planning time: `pnpm check` exit 0 (18 lint warnings, 0 errors);
`pnpm test` → 911 passed / 128 skipped / **0 failed**. The 128 skips are the
subject of plan 059 and are *not* a green signal.

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 043 | CI gate for `pnpm check` + `pnpm test`, and fix the timezone-coupled test that would flake on it | P1 | S | — | DONE — reviewer-approved after 1 revision round, **not merged**. Branch `advisor/043-ci-gate-and-tz-flake`, 4 commits (head `6a0f18e8`). Reviewer reproduced a **cold checkout** independently: `rm src/routeTree.gen.ts` → install → build (5 s) → check (18 warnings, 0 errors) → test (911/128/0). Workflow sets no `env:` key, so `DATABASE_URL` stays unset. |
| 044 | Gate the demo-persona cookie mint behind an explicit environment flag | P1 | S | — | DONE — reviewer-approved, **not merged** (operator decision). Branch `worktree-agent-a948c7728ea24e024`, commit `32a033ce`. Verified independently: 919 passed / 128 skipped / 0 failed, `pnpm check` 18 warnings 0 errors, 6 in-scope files only, real CI guard passes. |
| 045 | Wipe local engine state when the demo persona switcher changes identity | P1 | S | — | DONE — reviewer-approved, **not merged** (operator decision). Branch `worktree-agent-a507328d92952af1d`, commit `e73806a1`. Verified independently: 912 passed / 128 skipped / 0 failed, `pnpm check` 18 warnings 0 errors, 2 in-scope files only; executor also ran a negative control proving the new test fails without the fix. |
| 046 | Finish the Asia/Singapore date migration — calendar cells, engine write-side stamps, seed anchor | P1 | M | 043 (soft — its TZ pin makes the new tests meaningful) | DONE — reviewer-approved, **merged**. 997 tests at merge time, `pnpm check` 18 warnings; all three engine write sites now free of `getFullYear()` day-keying, and the `entry-date.constants.js` ↔ `entry-date.ts` parity pair exists. Executor found the plan's Step-7 case mis-specified (Base UI `ToggleGroup` treats re-clicking the selected cell as a *deselection*, so `onSelectDate` never fires) and re-anchored it, proving teeth: **4 of 5 cases fail against the pre-fix component under `TZ=Pacific/Auckland`, all 5 pass after.** It also reported 4 pre-existing Auckland failures in `history-sheet.test.tsx` and diagnosed them as the `realToday()` vs `sgToday()` mismatch — **which is exactly what plan 043 fixed**; the reviewer confirmed `realToday` is gone on merged main, so that criterion is satisfied post-merge. |
| 047 | Make reflection submit idempotent and the retry/sync lifecycle trustworthy | P1 | M | — | IN PROGRESS (opus executor, first wave on the real pushed main) |
| 048 | Stop writing student verbatim quotes and identity prose to server logs | P1 | S | — | DONE — reviewer-approved, **not merged**. Branch `advisor/048-scrub-pii-from-server-logs`, commit `e059fe65`. Verified: 921 passed / 0 failed (911 + 10 new), no `safety.matches` in any `console.*`, quote-bearing `summary` gone from every console arg. Design improved on the plan: `safetyLogFacts` types `matches` as `readonly unknown[]` so the helper *structurally cannot* read the student's words. Guard test includes meta-tests proving the heuristic still bites and stops at the closing paren. |
| 049 | Collapse the four stale same-origin copies onto the hardened helper; constant-time cron compare | P1 | S | — | DONE — reviewer-approved, **not merged**. Branch `advisor/049-consolidate-security-primitives` (= `worktree-agent-ac061f63e26edbd4e`), commit `49dfb1a7`. Verified: 927 passed / 0 failed (911 + 16 new), `pnpm check` 18 warnings 0 errors, exactly 1 `isSameOriginRequest` definition, all 5 target routes import the hardened helper, `timingSafeEqual` in, `=== \`Bearer` gone. No existing test needed a header added — the check was never weakened. Bonus: the realtime-mirror POST went from **no CSRF gate at all** to a 403 gate. |
| 050 | Response-hardening headers, with CSP report-only first | P2 | M | — | DONE-pending-preview — reviewer-approved, **merged** at `f1f941b4`. Steps 1–6 complete; 916→969 tests, `pnpm check` 18 warnings. Headers verified: `microphone=(self)` (voice capture preserved), enforcing CSP carries **only** `frame-ancestors 'none'`, full policy is report-only, `incompetech.com` in `media-src` so ambient audio survives, and `/share/(.*)` tightened to `no-referrer`. **Step 7 outstanding and cannot be done locally** — `vercel.json` headers do not apply under `pnpm dev`, so the report-only violation walk needs a preview deployment. |
| 051 | Bound and rate-limit the paid-inference entry points | P2 | M | 049 (same-origin on the realtime POST) | TODO |
| 052 | Close the verifier's full-match substring holes | P1 | S | — | DONE — reviewer-approved, **not merged**. Branch `advisor/052-verifier-full-match-holes` (= `worktree-agent-a82bce7bc164e8056`), commit `4d2ccf33`. Verified: 915 passed / 0 failed, `pnpm check` 18 warnings 0 errors, both **AE7 LOCKED** calibration tests + all three 0.8-threshold tests pass by name, `PARTIAL_MATCH_THRESHOLD` and `normalize` untouched. One approved deviation: 2 fixture lines in `test/ablation/sensemake-tools-off.test.ts` (out of the plan's in-scope list, but the new required counter field made the plan's own Step-2 gate unsatisfiable without it; no assertion changed). |
| 053 | Ground agent free-prose fields before they persist to identity pages | P2 | M | **052** | TODO |
| 054 | Connector: atomic claim + SQL-bounded selection + stop holding a tx across the 120 s agent call | P1 | L | 048 (file overlap — land 048 first) | TODO |
| 055 | Bound the boot wiki snapshot; stop shipping `raw_output_json` to the client | P1 | M | **054** (`queries.ts`) | TODO |
| 056 | Collapse the Growth tab's ~15 serial statements into 2–3; verify the index gap | P2 | M | 059 (soft — its gated tests are the real verification) | TODO |
| 057 | Memoize the History sheet's derived data | P2 | M | **046** (`CalendarPane.tsx`), **047** (`DayDetailCard.tsx` + `history-sheet.test.tsx`) | TODO |
| 058 | Batch capture tag writes into two statements | P2 | S | **047**, **054** (both edit `queries.ts`) | TODO |
| 059 | Revive the 128 permanently-skipped DB/handler tests | P1 | L | — | DONE — reviewer-approved after 1 revision round, **merged**. Main went **1006 → 1097 passing, skips 128 → 40**: **91 previously-dark tests now execute**, including the Connector auto-apply chain, verifier-gated confirm/forget, Cartographer dispatch, VIPS page loading and the counsellor brief. `pnpm check` 18 warnings, 0 failures. **Round 1 was rolled back**: its `dotenv/config` opened every Lane B gate on any machine with a `.env`, and on the operator's tree that meant tests connecting to a **live Neon database** as table owner (`23 failed / 1112 passed / 1 skipped`). The fix gates Lane B on a dedicated `TEST_DATABASE_URL`, and — the structural half — `test/setup.ts` now **deletes** an ambient `DATABASE_URL` when that opt-in is absent, so a test escaping its gate dies on "DATABASE_URL is not set" rather than silently reaching production. The executor proved the gate both **shuts** (simulated `.env`, census byte-identical) and **opens** (bogus `TEST_DATABASE_URL` on port 2 → every error `ECONNREFUSED 127.0.0.1:2`, so the opt-in wins over the ambient value), and added a canary asserting no ambient `DATABASE_URL` survives into the test process. **Reviewer re-verified on the operator's real tree with the real `.env`:** 1097/40/0, canary green, Lane B still skipped, and `pnpm test:db` now exits 1 rather than reporting a false-green all-skipped run. **Still deferred (no Postgres anywhere):** Step 2 and Step 7b — `test/db.test.ts` keeps `@ts-nocheck` and its 26 gated tests. **Plan 063's drizzle-orm gate therefore remains CLOSED.** Scope note: README gained a "Running the database-backed tests" subsection — outside the plan's literal file list, but the reviewer's revision brief asked for the variable to be documented and that is where the DB runbook lives. |
| 060 | Make the import-boundary guard cover every server function automatically | P2 | S | — | DONE — reviewer-approved, **not merged**. Branch `advisor/060-import-boundary-glob`, commit `1ee0f3fe`. Verified: hardcoded array gone, glob discovers all 24 `*.functions.ts`, `serverOnlyExemptions` empty, 1 file changed, 3 tests pass. Negative control run twice against the committed file (injected `~/db/client` → guard failed with the expected message → reverted), so the widened guard is proven to bite rather than pass vacuously. |
| 061 | Typed sidecars for the capture-facing engine slices + one canonical capture type (**flagship DRY plan**) | P1 | M | 047 (soft — it touches capture fields) | TODO |
| 062 | Migrate `@base-ui-components/react` (deprecated) → `@base-ui/react` | P2 | M | — | IN PROGRESS (opus executor, first wave on the real pushed main) |
| 063 | Bump `@anthropic-ai/sdk` + patch the `ws` advisory; defer `drizzle-orm` | P2 | S–M | — (drizzle part gated on **059**) | TODO |
| 064 | Fix stale pointers agents trip over — README plan entry-point, `followups.md`, ghost config refs, **`CLAUDE.md`'s wrong Sheet primitive** | P2 | S | — | DONE — reviewer-approved, **not merged**. Branch `advisor/064-docs-config-drift` (= `worktree-agent-a66022dcb34090812`), 4 commits. Verified: 5 files, `git diff -- src/ test/` empty, 911/128/0 unchanged, `biome.json` still valid JSON, CI guard **job body** untouched (only 4 comment lines removed), and all 8 symbols in the new `CLAUDE.md` block exist in `src/components/ui/sheet.tsx`. **Two plan defects found and correctly refused, not worked around** — see the note below. |
| 065 | Runner timeout terminal + unknown stop reasons classified + hydration not hanging | P2 | S | — | DONE — reviewer-approved, **not merged**. Branch `advisor/065-runner-and-hydration-small-fixes`, 3 commits (head `ba70c25f`). Verified: 920 passed / 0 failed (911 + 9 new), duration 9.87s (baseline ~10s — the timeout test did not balloon it), `git diff -- src/server/` **empty** so the auto-connector mapping is provably untouched, all greps pass. Executor confirmed both fixes fail pre-fix (the timeout test reproduces the actual hang). Caveat: each `runManagedAgent` call now leaves one stray 1 s cleanup timer. |
| 066 | **Direction**: productionize capture-time Connector — measured latency, real snapshot seam, debounce, acknowledgment beat | P1 | M | 054 (soft) | DONE — reviewer-approved, **merged**. 1006 tests, `pnpm check` 18 warnings. `window.__studentSpaceGame` read is gone from the bridge (0 occurrences) and the `applySnapshot` seam is threaded from `EngineHost`; **065's hydration fix and 066's seam coexist correctly on main** (verified: 0 old short-circuits, 1 seam). 10 new tests including the three debounce cases (burst of three capped at one in-flight + one rerun) and, critically, *dispatches nothing when the run fails — silence is the failure UX*. **Step 1 (measure live latency) remains BLOCKED** on missing `DATABASE_URL` + `MANAGED_AGENT_*`; the executor recorded the failed preconditions rather than fabricating numbers, so "seconds" is still an unmeasured product claim. **Two product calls left to the operator:** the ack copy duplicates `IslandProgressionOverlay.tsx:80`, so a flagged capture can show the same sentence twice (stacking, distinct toast id — reusing `PROGRESSION_TOAST_ID` would make it replace instead); and the ack renders only on `/` because sonner draws every toast in every mounted Toaster, so a capture-then-navigate race swallows it. |
| 067 | **Direction**: decide and build the counsellor surface — or delete the dead brief pipeline | P2 | M | — | DONE (decision documented; correctly stopped at its Step-2 halt) — **merged**, doc at `docs/solutions/2026-07-25-counsellor-surface-decision.md`. **The executor corrected this plan on its decisive point, and the reviewer verified all three corrections.** (1) A student-initiated, revocable, quotes-**off**-by-default adult-facing surface *already ships* — the share-token lane (`ProfileSheet` Share → `/api/share/create|revoke|redactions` → `/share/$token`, `showQuotes = false` by default). It landed 8 days *after* the brief renderer, so **the brief was superseded, not forgotten** — which reframes the whole finding. (2) The `world_mailbox` field is a live call on a **dead field**: nothing outside its producer reads it, and `unreadBriefCount` is hardcoded 0. (3) `counselor_students` is the tenancy envelope, not a latent portal. **Recommendation: DEFER** — building the download as specified would ship verbatim quotes with no toggle, a privacy *regression* against the share lane; deleting would lose the only Trajectory rendering and the personality diagnostic guard. Fold Trajectory into the existing share lane instead. **Awaiting the operator's call.** |
| 068 | **Direction**: decide what `/letters` is — real correspondence or honestly-scoped prompts | P2 | S–M | **067** (settles who the adult is) | TODO |
| 069 | **Default the world's sound to muted** (operator request during live demo testing) | P1 | S | — | DONE — reviewer-approved, **not merged**. Branch `advisor/069-default-sound-muted`, commit `e2b827c3`. Verified: 914 passed / 0 failed (911 + 3 new), `pnpm check` 18 warnings, `src/components/` untouched (0 files) so the HUD button cannot have regressed. **Verified live in a browser** against a dev server booted from the worktree: no stored pref → starts muted; `'0'` → still unmuted (a user who opted in is not re-muted); `'1'` → muted; and the toggle round-trips muted→on→muted from the new default. Executor caught that `localStorage` is undefined under happy-dom, which would have made its test pass vacuously via the `catch` branch, and installed the repo's existing storage-stub pattern. |
| 070 | **Fix the load-sensitive `islandSpecCore` timeout flake** (written mid-execution: four executors hit it, and 043's new CI gate makes it the likeliest red run) | P1 | S | — | DONE — reviewer-approved, **merged**. 1097/40/0, `pnpm check` 18 warnings, one file, +11/-1. **The executor disproved the plan's diagnosis and was right to.** The plan claimed the search revisits cells and could be memoized; it measured **896 predicate calls, 896 distinct cells, 0 cache hits**, then found why in `snapToLand.ts:78-93` — the `Math.max(Math.abs(dc), Math.abs(dr)) !== radius` guard makes the Chebyshev rings disjoint, so each cell is visited at most once. The cost is **inherent, not redundant**; the exact-coordinate fallback fails identically since cell-keying is strictly coarser. **Reviewer verified this in the source.** It reverted, committed nothing, and refused to write a commit message that would have been a false claim in git history. **It also reproduced the flake:** three full-suite runs under 10 busy cores gave **4364 / 4614 / 5472 ms** — the worst *exceeds* vitest's 5000 ms default, so that run would have gone red. The shipped fix is the 20 s per-test ceiling only (~3.7× headroom), with the measured range and the negative memoization result written into the code comment so nobody retries the cache. Global `testTimeout`, the assertions and the `-20`/`0.5` start coordinates are all untouched. **Open product question, deliberately not chased:** whether scanning 896 cells from a distant start is itself excessive is a question about the search, not the test. |

**Suggested execution order.** Numeric order works, with these constraints:

- **Start with 043** — it is the verification baseline every later plan's
  "green gate" claim depends on. It deliberately does **not** set
  `DATABASE_URL` in CI; doing so would turn 059's 128 silent skips into 128
  hard failures.
- **Security quick wins next** (044, 045, 048, 049, 052) — all S, all
  independent, all high-leverage.
- **Hard file-overlap chain**: 048 → 054 → 055 → 058, and 047 → 058, and
  046 → 047 → 057. Do not run these in parallel worktrees.
- **Three plans generate a Drizzle migration** (047, 054, 056). Each says to
  use whatever number `drizzle-kit` assigns after rebasing — do not hardcode a
  migration filename, and inspect the generated SQL by hand where a plan says
  a CHECK constraint is being widened (054).
- **053 after 052** (shared verifier + calibration fixtures).
- **068 after 067** (067 decides who the adult is; 068 decides the channel).
- 059 is L and independently landable step-by-step; it unblocks the real
  verification story for 056 and the deferred `drizzle-orm` bump in 063.

Suite interaction notes:

- 046 and 043 interact: 043 pins a non-SGT `TZ` in the vitest config, which is
  what makes 046's timezone regression tests meaningful rather than
  vacuously green on an SGT machine.
- 052/053 both touch `src/agents/verifier.ts` and its AE7 calibration pair.
  In **both** plans a calibration regression is a STOP condition, never a
  licence to re-pin the fixtures.
- 054 is the suite's hardest plan: it must land the transaction split and the
  atomic claim *together*, because splitting the transaction removes the
  read/write atomicity today's implicit claim relies on. **Its claim mechanism
  was redesigned during planning**: a row lock (`FOR UPDATE SKIP LOCKED`)
  cannot span the split because the lock dies with its transaction, and a
  transient `status='pending'` claim row would flash a payload-less diff at the
  student (`load-pending-review.handler.server.ts:34-35` renders the newest
  pending diff, and its R30/AE8 comment asserts at most one exists). The plan
  therefore adds a `'running'` status so the claim row *is* the audit row —
  which also makes one new `UNIQUE (student_id, mirror_entry_id) WHERE status
  <> 'forgotten'` index serve as both claim arbiter and duplicate backstop.
- **Follow-up recorded, not planned**: `run-cartographer.handler.server.ts` has
  the same transaction-spans-agent-call anti-pattern with a **780 s** runner
  timeout, but shares no helper with the Connector path (different context
  builder, never touches `vips_proposed_diffs`). 054 Step 1 greps to confirm
  that separation and STOPs if it no longer holds.
- 066 (client-side capture-time Connector) and 054 (server-side coordination)
  are complementary; 066's debounce is worth keeping even after 054 lands.
- **Corrected finding, recorded so nobody rebuilds it**: the audit initially
  reported that letters have no path into a prompted capture. They do —
  `LettersSheet.tsx:110-127` deep-links a letter's prompt into the Ask
  overlay, and `AskSheet.tsx` stamps `letterId` onto the capture
  (`KNOWN_CAPTURE_KEYS` includes it). The real gaps, per plan 068, are that
  letters have no backing store and that `letterId` never reaches the server.

Facts discovered *during planning* that the audit missed (each verified, each
folded into the plan named):

- **`CLAUDE.md`'s "Sheet primitive" block (lines 51–66) is wrong** and would
  make an agent write non-compiling code: `src/components/ui/sheet.tsx` imports
  no Base UI (its own line 20 says "Plain framed div — not a Base UI Dialog"),
  exports no `Sheet`/`SheetSurface`, and has no `open`/`modal`/`onOpenChange`
  prop — routed sheets compose `<PageSurface>` plus the `usePageEscape` hook.
  Line 31's "Base UI for behavior" bullet *is* still accurate (11 other files
  use it). → **plan 064 Step 5**, and it is why **062**'s manual checklist
  targets the capture drawers/chooser/radio-group rather than routed sheets.
- **`Captures.js` declares `patch()` twice** (`:117` and `:153`; the second
  silently wins, so the first is dead). Flagged in **047** and **061**,
  deliberately not fixed by either — it wants its own tiny change once 047's
  characterization tests exist.
- **`FORCE ROW LEVEL SECURITY` is not emitted** (`src/db/migrations/README.md`),
  so a table-**owner** connection bypasses RLS. Without this caveat **059**'s
  "RLS failure = critical security finding" STOP would fire spuriously on any
  local Postgres; the plan now inlines a diagnostic query.
- **`open_question` is persisted completely unchecked** — both
  `auto-connector` and `confirm-diff` apply the safety denylist only to
  `compiled_truth_rewrite`. → folded into **053**, along with the DRY win that
  `checkCompiledTruthForDimension` is duplicated verbatim in both handlers.
- **`GroupBy` + `pickDominant` trap**: `pickDominant` starts `topCount = -1`,
  so feeding it zero-count rows returns a dimension where today's filtered
  `GROUP BY` (which omits zero groups) returns `null` — it would mislabel the
  "Dominant dimension" tile. → **056** makes the zero-filter mandatory and
  tests it without a database.
- **`GrowthPane` reads `sprouts.years()`**, but `sprouts` is not one of the
  three subscribed slices — it only updates today because everything
  re-renders. → **057** makes threading `capturesVersion` mandatory and
  comments it as load-bearing, since it looks like dead code.
- **The realtime prewarm mints a real session** on CaptureFab hover
  (`CaptureFab.tsx:103`) and every AskSheet open (`AskSheet.tsx:307`), so
  sessions vastly outnumber recordings. → **051** sizes its ceiling at
  120/hour around that amplification rather than the 20/hour first proposed.
  (That prewarm also bills for unrecorded OpenAI sessions — a real cost
  question, recorded but out of scope.)
- **`KNOWN_EVENT_KEYS` is `{id,label,kind,date}`**, so calendar events can
  never carry `entryDate`/`title` — making those fallbacks in
  `eventDate`/`eventLabel` dead. → **061** types to the merger's real shape,
  keeps the fallbacks (types-only plan), and reports.
- **All 8 files that plan 060 newly covers are already clean at HEAD**, so the
  widened guard goes green immediately — which is why 060 adds a mandatory
  *negative* test (inject a violation, confirm the guard fails, revert) to
  prove the widening actually bites.
- **`ws` reaches production via `openai/realtime/ws`** (nothing in `src/`
  imports `ws` directly), and `pnpm why ws` shows one resolved 8.20.0 shared by
  both paths — so **063** needs a single bump and **no pnpm override**.

#### Execution log (2026-07-25)

First wave dispatched as Opus executors in isolated git worktrees, each
reviewed by the advisor (done criteria re-run independently, diff scope-checked
hunk by hunk, new tests read for substance). **Nothing has been merged — that
is the operator's call.** Branches live under `.claude/worktrees/`.

- **044 APPROVED.** 919 passed / 0 failed (baseline 911 + 8 new), clean tree,
  exactly the 6 in-scope files. The tests assert real behavior, including
  `expect(mocks.getCookie).not.toHaveBeenCalled()` — proving the flag
  short-circuits *before* the cookie read, not merely that it returns null —
  and a byte-exact `Set-Cookie` assertion for the flag-on path.
- **045 APPROVED.** 912 passed / 0 failed, 2 in-scope files. The regression
  test asserts `mock.invocationCallOrder` (dispose < clear < submit), which is
  the actual bug; the executor additionally ran a negative control confirming
  the test fails without the fix.
- **043 INCOMPLETE** — killed by the account session limit mid-run, not by a
  STOP condition. Substance of steps 1–2 is correct and independently
  verified. **Carry-forward finding: the TZ pin is safe** — the full suite
  passed twice under `TZ=America/New_York` with zero failures, retiring the
  plan's main risk (its STOP condition was ">3 test files fail under the
  pinned TZ"). Remaining for a continuation executor: re-run `pnpm format`
  (Biome wants the now-shorter `events: [...]` array collapsed onto one line
  at `history-sheet.test.tsx:306`), create `.github/workflows/ci.yml`, commit.
  The executor also observed one transient timeout-adjacent flake that
  re-ran green — worth a look when wiring CI, since a test near its 5 s
  timeout is a CI-stability risk.

**Wave 2 (2026-07-25, later): 043, 048, 049, 052, 060, 064, 065 — all seven
APPROVED.** 043 took one revision round; the other six passed first time.
Combined, the nine approved branches add 8 + 1 + 10 + 16 + 4 + 2 + 9 = 50 tests
and zero failures.

**MERGED 2026-07-25.** All ten approved branches landed on `main` with
`--no-ff` (preserving each executor's per-step commits and rationale), in
numeric order, **zero conflicts**. Safety tag `pre-advisor-merge-2026-07-25`
marks the pre-merge commit `031d1974` — roll the whole batch back with
`git reset --hard pre-advisor-merge-2026-07-25`.

The combination was verified *after* merging, which no individual branch could
prove: `pnpm check` exit 0 with 18 warnings, and `pnpm test` **964 passed /
128 skipped / 0 failed** — exactly the predicted 911 + 53 new tests, with the
skip count unchanged. Post-merge spot checks: CI workflow present with
build-before-check and no `DATABASE_URL`; sound defaults to muted; the demo
mint is gated; exactly one `isSameOriginRequest` definition survives; and
`CLAUDE.md` no longer claims the sheet primitive is a Base UI Dialog.

Merge commits: 043 `734785df` → 044 `881546a1` → 045 `a3072410` → 048
`5a506190` → 049 `bf2f7ee0` → 052 `678e2225` → 060 `2bd04fea` → 064
`ab765d04` → 065 `0d354e53` → 069 `741b9467`.

**Operator action still outstanding:** set `ENABLE_DEMO_PERSONAS=1` in the
Vercel demo environment, or 044 makes the deployed "Use a demo account"
button return 404.

#### Cross-cutting facts every future executor and plan needs

1. **`src/routeTree.gen.ts` is the fresh-checkout trap.** It is gitignored
   (`.gitignore:23`), 645 lines, and generated **only** by `tanstackStart()` in
   `vite.config.ts` — `vitest.config.ts` does not load that plugin, there is no
   `tsr`/router CLI in `node_modules/.bin`, and there is no
   `prepare`/`postinstall` hook. So in any fresh worktree (and in CI)
   `pnpm check` fails with `TS2307: Cannot find module '~/routeTree.gen'` plus
   ~12 `TS2345`s, and `pnpm test` fails 2 files (898 passed). **All six wave-2
   executors hit this.** Remedy, measured: `pnpm build` regenerates it in
   ~5 s. Plan 043 now puts that step in CI with a "LOAD-BEARING" comment. Every
   future plan should carry this in its environment note.
2. **`test/engine/islandSpecCore.test.ts > snapToLand > never lands on a
   pre-occupied cell` is a real, load-sensitive flake** — not timezone-related.
   Four independent executors hit it. Best characterisation (from 043's
   executor): failed 4 of 9 full-suite runs, and it correlates with wall-clock
   load, not content — every failing run took 18.7–23.0 s, every passing run
   ~10–12 s; it passes standalone in 1.5–3 s under the TZ pin, and the file
   references no date/timezone API. A shared GitHub runner is slower than this
   laptop, so **this is the most likely source of CI flake once 043 lands.**
   Worth its own small plan: raise that test's timeout or reduce its work.
3. **Agents in concurrent worktrees must never use `git stash`.** The worktrees
   share one `.git`, so the stash stack is repo-global, not worktree-scoped.
   The 048 and 060 executors raced: 048's `pop` took 060's entry. Both detected
   it, repaired it independently, and cross-verified — **no work was lost**, and
   the reviewer confirmed every worktree is clean and both commits are intact.
   Two entries remain on the shared stack: 048's now-redundant `plan048-wip`,
   and a **pre-existing `feat/island-editor-import-lane` stash that predates
   this session and must not be dropped.**

#### New findings surfaced during execution (not yet planned)

- **Possible second PII path, unverified** (from 048's executor):
  `src/server/persist-mirror.handler.server.ts:64` throws
  `new DiagnosticLanguageError(safety.matches)` and
  `src/server/edit-wiki.handler.server.ts:29` builds an error message embedding
  matched text. Nobody traced whether those errors reach a logger. If they do,
  it is the same class of leak 048 just closed. Worth a short investigation.
- **Plan 064's `CURRENT_STATE` criterion is unachievable as written** — 12
  further files under `docs/plans/`, `docs/plans/_archive/` and `docs/ideation/`
  still reference it, and the plan's own Scope forbids touching historical
  archives. The executor correctly refused to edit them. Restricted to
  in-scope files the criterion passes. Narrow the criterion, don't widen the
  scope.
- **Plan 064 is self-contradictory** on `followups.md`: its Step 2.8 mandates a
  provenance note naming four deleted files, while its done criteria grep for
  zero hits of those names. The executor kept the mandated note (the criterion's
  *intent* — no live pointer to a deleted file — is satisfied; a blockquote
  provenance line is not a pointer). Reword one or the other.
- **Plan 065 contained a literal that failed its own grep criterion** (the
  recommended comment text included `backend.refreshSnapshot?.().then(...)`,
  which the done criteria forbid appearing in the file). Executor reworded the
  comment. Same class of defect as the two above: **done criteria written as
  bare greps collide with plan text that quotes the old code.**

**Plan defect found during review (fix before re-running 044):** 044's done
criterion `grep -rn 'DEV_BYPASS_AUTH' src/ | grep -v 'src/auth/middleware.ts'
→ no output` is too loose and fails on 6 *pre-existing* prose mentions. The
real CI guard greps `process\.env\.DEV_BYPASS_AUTH`, which passes. The
criterion should match the guard's pattern.

Findings considered and rejected / deferred (2026-07-25 audit — don't re-audit):

- **`drizzle-orm` 0.36 → 0.45** (high advisory: SQL injection via improperly
  escaped identifiers, fixed 0.45.2) — **deferred, not ignored**. Verified no
  reachable sink today: every value in `src/db/client.ts` / `queries.ts` is
  parameter-bound, and the only `sql.raw` uses are static CHECK-constraint
  literals in `schema.ts`. Gated on 059 restoring DB test coverage; trigger
  recorded in plan 063.
- **Six build-tooling high advisories** (`undici` via `cheerio`, `vite`,
  `postcss`, `js-yaml` via `xmlbuilder2`) — all resolve through
  `@tanstack/start-plugin-core` build tooling, not the request path. Leave to
  the normal TanStack upgrade cadence.
- **Island mesh build allocations** (~790k short-lived objects across three
  call sites in `islandSpecCore/terrainGrid.ts`, three per vertex at
  `SEGMENTS = 512`) — real but boot-once, not per-frame, and the numeric
  output is pinned by exact test expectations plus documented
  feature-preservation floors. Profile boot before touching. **Verdict: not
  worth doing on its own.**
- **`Rain.update()` per-frame array literals** (three per frame, one a
  compile-time constant) — ~180 small arrays/second while raining; trivial for
  a generational GC. Hoist only if already editing that function. **Verdict:
  not worth doing.**
- **`WorldInteractions.tsx` size** (1967 lines, mixes React presentation with
  imperative engine glue, reaches `OverlayController.getInstance()` at 6
  sites) — extraction is safe but the pain is inferred, not observed. Act when
  a feature forces you into the file; land 061's typed seam first. The two
  `_dev.*` route files (2727 + 1667 lines) are dev-only and explicitly **not
  worth splitting**.
- **`OverlayController.js` as "compatibility bridge"** — checked and the
  `CLAUDE.md` framing is accurate, not stale: 146 lines, one `Map`, 3 body
  classes, 3 non-routed keys. The only cleanup worth doing is folding
  `WorldInteractions`' 6 `getInstance()` sites into `game.openSurface`; too
  small to plan.
- **Verifier and agents-runner error-path coverage** — audited and found
  adequate (`test/agents/verifier.test.ts` 463 lines incl. the AE7 calibration
  pair; `managed-mirror.test.ts:154-290` drives timeout, stream error, and
  each retry branch against a fake transport). No finding.
- **`/share/$token` unauthenticated read path** — by design and carefully
  implemented (`share_token_resolve_with_status` SECURITY DEFINER as the single
  unauth read, 128-bit tokens, `show_quotes` read only from the DB row,
  strict-mode Zod rejecting a client override, `noindex` set). No finding.
- **`getDbForMemoryModule` bypassing `withStudent`** (`queries.ts:530`,
  `agents/memory/index.ts:230,308`) — the documented non-RLS mapping-table
  lookups; re-establishes the GUC inside its own transaction. By design.
- **`_app.tsx:28-37` failing open on `loadAuthMenu` errors** — every
  downstream handler independently calls `requireCounselorContext`. By design.
- **`withStudentLegacy`** (`src/server/tenancy.server.ts`) — zero remaining
  callers in `src/`; note that `test/tenancy.test.ts` tests *this*, not the
  real envelope, which is why 059 matters. Too small to plan on its own.
- Still-standing deferrals from the 2026-07-23 audit (per-frame raycast
  broad-phase, History Growth-tab client fetch waterfall, GLB preload hints,
  duplicate `reviewText` writers in `AskSheet.tsx`, console-noise gating) —
  unchanged; see that suite's list above.

Island-editor dependency notes:

- **Recommended order: 014 → 015 → 016 → 017.** 014 and 016 both edit
  `specIO.ts`'s migration path; 016 and 017 both edit `App.tsx`/`icons.tsx`
  and 017's spec tests assume 016's v5 bump — run them sequentially, not in
  parallel worktrees.
- 015 is pure asset pipeline (no `src/` changes); 016 needs its `grass.glb`,
  017 needs its `character.glb` + clip-name contract test.

## Reconcile log

**2026-07-23 (HEAD `a9e1364e`)** — full backlog reconcile; nothing TODO /
BLOCKED / IN PROGRESS remains. Verified DONE on current main:

- **030** — `SEGMENTS = 512` holds (`Game/View/islandGeometry.ts:40`).
- **031** — saved spec is 128×128 (`Game/Data/defaultIslandSpec.json` grid
  cols/rows = 128).
- **032** — `BLUR_PASSES = 4`, `BLUR_MIX = 0.85` hold
  (`Game/State/islandSpecCore/terrainGrid.ts`).
- Gates: `pnpm check` passes (18 lint warnings, 0 errors);
  island/spec-core/colorspace tests 58/58; `pnpm check:island-editor`
  242/242 (covers the 014–029 editor suite).
- 001–012 (Character Studio) live on `origin/feat/character-studio`
  (branch present); not re-verified per-plan — unchanged since 2026-07-04
  handoff.

**Out-of-scope observation** (not plan drift, needs owner attention): the
full `pnpm test` run has **10 failing tests in 5 files**, all in recent
product-UI work, none touching any plan here — `history-sheet.test.tsx`
(5, e.g. "Unable to find /Friday, April 3, 2026/"),
`trajectory-sheet.test.tsx`, `dev.pipeline.test.tsx` (2),
`student-space-host.test.tsx`, `edupass-login.test.tsx`. Likely fallout of
the 2026-07-23 history-sheet commits (`42e16752`/`8601aac5`), which are
already pushed to main.

## Findings considered and rejected

See `000-architecture-and-strategy.md` §3 for the full rejected-alternatives
table (Babylon.js, WebGPU-first, VRM-as-format, procedural generation,
PBD cloth, Draco, retargeting, Spline embed, photoreal PBR) — do not
re-litigate these in execution or review without new evidence.
